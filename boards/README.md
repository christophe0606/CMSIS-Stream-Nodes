# README

Provides board configurations:

* How to init the hardware peripheral to be used by the nodes
* Device tree overlays

etc ...

It does not provide the code that is needed to build for a given board.
So, the runner build may have to be modified to include and build the required drivers.

